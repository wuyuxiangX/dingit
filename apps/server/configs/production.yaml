app:
  name: dingit
  env: production

server:
  port: 8080

logger:
  level: info
  format: json

cors:
  allowed_origins:
    - "*"

rate_limit:
  enabled: true
  requests_per_second: 10
  burst_size: 20
