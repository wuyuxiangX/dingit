name: notify_cli
description: Notify Hub CLI - Send and manage notifications from the terminal
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

executables:
  notify: notify

dependencies:
  args: ^2.7.0
  http: ^1.2.0
  yaml: ^3.1.0
  notify_shared:
    path: ../../packages/notify_shared

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
